name: test ci
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest

    env: 
      ssh_key: ${{ secrets.SSH_KEY }}

    steps:
    - uses: actions/checkout@v3
    - run: mkdir -p ~/.ssh
    - run: echo $ssh_key > ~/.ssh
    - run: ssh bdb2c2023q4_guest_3@brain-client.bigdatateam.org ls
    - run: python3 test.py
    - run: echo "OK"
